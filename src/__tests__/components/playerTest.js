import React from "react";
import {render, fireEvent} from "@testing-library/react";
import { Player } from "../../components/player/Player";

describe('<Player />', () => {
    it('renders the <Player /> with a video of bunny', () => {})
})